# Full Antigravity Flow Prompt

Paste this into Antigravity Manager if slash commands are not registered.

```text
We are adding dynamic ingestion support to Retriva Gateway.
Use the SDD artifacts in specs/dynamic-ingestion-gateway and memory/constitution.md.
Proceed in four phases:

1. DEFINE: inspect repo, refine spec, produce definition-review artifact.
2. ARCHITECT: inspect current Gateway architecture, produce recon and task graph, refine plan.
3. EXECUTE: implement source CRUD, source lifecycle, run tracking, MediaWiki config validation, connector manager skeleton, internal worker API if appropriate, and tests.
4. VERIFY: run tests/checks, review security constraints, produce verification and security artifacts.

Do not log or persist document content or credentials. Preserve static ingestion behavior. Gateway remains the policy choke point. Core remains the canonical ingestion pipeline.
```
